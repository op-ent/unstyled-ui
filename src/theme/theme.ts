import { DEFAULT_BUTTON_THEME as button } from '../components/Button/Button.theme'
import { DEFAULT_BUTTON_GROUP_THEME as buttonGroup } from '../components/ButtonGroup/ButtonGroup.theme'
import { Theme } from '../types'

const theme: Theme = { button, buttonGroup }

export default theme
